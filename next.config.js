/** @type {import('next').NextConfig} */
const nextConfig = {
  // O'z IP manzilingizni yoki domainingizni qo'shing
  allowedDevOrigins: ['*'],
  output: 'standalone',
};

module.exports = nextConfig;
